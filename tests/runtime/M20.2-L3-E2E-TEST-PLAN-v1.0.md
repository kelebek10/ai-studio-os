# M20.2 L3 Controlled E2E Test Plan v1.0

## Objective
Validate the existing Telegram command gateway contract before attempting a live Telegram/n8n message.

## Preconditions
- Non-production repository/runtime only.
- Telegram gateway contract is present.
- No production mutation.
- Test uses synthetic/unit harness data.

## Executed evidence
Command:
`PYTHONPATH=. python3 tests/runtime/test_telegram_gateway.py`

Observed:
`M16.10 GATEWAY UNIT HARNESS: PASS`

Coverage:
- allowed `/status`
- unauthorized chat rejection
- forbidden command rejection
- `/approve` Human Gate enforcement
- `/pause` Human Gate path
- empty message rejection

## L3 live-test gate
A live Telegram/n8n test is permitted only after confirming:
1. the target workflow is the intended non-production test workflow;
2. no production migration/configuration is required;
3. inbound command reaches the gateway contract;
4. outbound response remains attributable to the originating test command;
5. execution/audit evidence is retained.

## PASS/FAIL
PASS requires evidence for the complete inbound-to-outbound path.
Any gateway bypass, unauthorized command acceptance, missing audit evidence, or production mutation is FAIL/BLOCKED.

## Next action
Inspect the n8n workflow and its execution contract, then run one controlled `/status` smoke message if the workflow can be unambiguously identified as the test path.

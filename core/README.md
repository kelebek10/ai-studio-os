# Core

The authoritative domain layer of PAI-FORGE.

Core contains validated knowledge, deterministic rules, structured data,
provenance records, and approved decisions.

Architecture principle:

Structured-first → Semantic-second → LLM-last

Unverified external inputs must not become authoritative Core knowledge.
